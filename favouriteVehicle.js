//TASK 9 PART 3

const carNames = ['Ford', 'Toyota', 'Honda', 'Tesla', 'BMW'];

for (let i = 0; i < carNames.length; i++) {
  if (carNames[i] === 'Tesla') {
    console.log(`The ${carNames[i]} is my favourite car`);
  } else {
    console.log(`The ${carNames[i]} is not my favourite car`);
  }
}
